classdef TemperatureBiasCalculator
    properties
        A
        B
    end

    methods
        function result = CalculateBias(self)
            % call all the different functions in the different files
            test()
        end
    end
end